public class NodeInfo { // so that this type can be returned
        String key; // add other fields as needed!

        NodeInfo() {
                key = null;
        }

        NodeInfo(String keyIn) {
                key = keyIn;
        }

}

